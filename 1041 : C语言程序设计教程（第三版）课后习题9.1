Description
定义一个带参的宏，使两个参数的值互换，并写出程序，输入两个数作为使用宏时的实参。输出已交换后的两个值。
Input
两个数，空格隔开
Output
交换后的两个数，空格隔开
Sample Input
1 2
Sample Output
2 1

#include<stdio.h>
#define JH(X,Y) c=X,X=Y,Y=c 
int main()
{
	int a, b, c;
	scanf("%d%d", &a, &b);
	JH(a, b);
	printf("%d %d", a, b);
	return 0;
}
